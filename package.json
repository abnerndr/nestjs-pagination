{
	"name": "typeorm-paginate-nestjs",
	"version": "0.0.1",
	"main": "dist/index.js",
	"author": "<abnerndr.dev@gmail.com>",
	"license": "ISC",
	"description": "",
	"scripts": {
		"build": "npx rimraf dist && tsc && cpx 'src/**/*.ts' dist",
		"lib:login": "cross-env npm login --user $NPM_USER --password $NPM_PASS",
		"lib:publish": "npm run build && npm version patch --no-git-tag-version && npm publish --access public"
	},
	"devDependencies": {
		"@types/node": "^20.12.2",
		"cpx": "^1.5.0",
		"cross-env": "^7.0.3",
		"rimraf": "^5.0.5",
		"tsc": "^2.0.4",
		"typescript": "^5.7.2"
	}
}
